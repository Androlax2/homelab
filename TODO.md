- Also for all the other services I've on the homelab, how could I version all the settings I've everywhere so If I've any issues I can get everything back running easily and everything
  get in sync from the UI If I change any, I want to have a 2 way sync also

- Add a workflow to automatically update all PRs (keeping all branches up to date)
- Manage the environment secrets of the NAS inside github settings secrets

- Switch configuration of Sonarr/Radarr to Trash IDs (for most of the stuff that could be it)
