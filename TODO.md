- Look to make a sync from the NAS setup (Sonarr, radarr, etc...) to the repo for the config of each services, so the repo always have 
  the correct config.
- Manage the environment secrets of the NAS inside github settings secrets
